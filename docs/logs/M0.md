# M0 工程基座 — 实现日志

- **阶段**：M0 工程基座
- **状态**：已完成
- **完成标准**：`make lint test dev` 三件套全绿 ✅（ruff + mypy + pytest 均通过）
- **实现计划**：`docs/plans/M0-implementation-plan.md`

## 改动点

1. 建立工程骨架：`pyproject.toml`（依赖锁定 + 三个 entry_points group 占位）、`requirements.txt`、`requirements-dev.txt`、`.env.example`、`Makefile`（install/lint/test/dev）、`.pre-commit-config.yaml`
2. 实现 `src/aiops_apm/` 基座：
   - `settings.py` — `Settings(BaseSettings)`，`env_prefix="APM_"`（pydantic v2 `SettingsConfigDict`）
   - `exceptions.py` — `ErrorCode`（7 值）+ `AppException(code, reason, trace_id)`
   - `_app.py` — `create_app(settings=None)` + `lifespan`（空壳 + TODO）；统一异常处理覆盖 `AppException` / Starlette `HTTPException` / 兜底 `Exception`，均返回 `{code, reason, trace_id}`
   - `router/api.py` — `GET /health`（200）、`GET /ready`（未就绪 503 NOT_READY）
3. 测试（TDD 三用例）：`tests/test_health.py`（UC-0.1）、`tests/test_settings.py`（UC-0.2）、`tests/test_exceptions.py`（UC-0.3），共 8 用例

## 新增文件清单

```
pyproject.toml
requirements.txt
requirements-dev.txt
.env.example
Makefile
.pre-commit-config.yaml
src/aiops_apm/__init__.py
src/aiops_apm/settings.py
src/aiops_apm/exceptions.py
src/aiops_apm/_app.py
src/aiops_apm/router/__init__.py
src/aiops_apm/router/api.py
tests/__init__.py
tests/conftest.py
tests/test_health.py
tests/test_settings.py
tests/test_exceptions.py
docs/plans/M0-implementation-plan.md
docs/archive/M0-skeleton.md
docs/logs/M0.md
```

## 修改文件清单

- `CLAUDE.md` — 当前里程碑状态更新；实现流程规则新增「实现计划落库 `docs/plans/`」
- `docs/apm-alert-implementation-plan-enhanced.md` — M0 小节标记「已实现」，指向归档
- `README.md` — 实现进度表与状态更新

## 遗留问题 / 后续注意

- `/ready` 目前恒 503（M0 空壳未构建 storage/registry）；后续里程碑在 `lifespan` 构建 `app.state.storage`/`app.state.registry` 后自动变 200
- `pytest` 运行时有 Starlette 的 `httpx` 弃用告警（第三方 `fastapi/testclient` 引入），不影响测试结果；后续可评估切换 `httpx2` 或过滤该告警
- 前端（HealthPage / ErrorPage / SPA 壳）本次未实现，技术栈待定后另行补充
- `_app.py` 中 lifespan 的 TODO 标注了 M2（连接池）、M4（插件注册）、M6（scheduler）的填充点
